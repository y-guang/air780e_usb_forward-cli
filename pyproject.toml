[tool.poetry]
name = "air780e-usb-forward-cli"
version = "0.1.0"
description = ""
authors = ["spike <guang.spike.yang@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
